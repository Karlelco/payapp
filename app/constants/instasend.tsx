'use client';
import { Button } from '@nextui-org/button'

export default function MyScreen(){

    async function test (){
  }
return(
  <div>
  	<form action={test}>
      <Button >Pay Now</Button>
    </form>
  </div>
)
}
